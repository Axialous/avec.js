@style [
    .titre {
        text-wrap: balance;

        --l-fonte: 1.5em;
        --p-fonte: var(--p-titre);
    }

    .titre.centre {
        text-align: center;
    }
]

@args [$type : classique]

<h1
    .titre 
    [$type = centre] ? .centre
>
    @stud

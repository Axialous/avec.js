@style [
    .champ-texte {
        --l-ecart: 0.5rem;
    }

    .champ-texte  > input {
        width: 100%;
        height: 2rem;

        border: 0.125rem solid var(--c-vif);
        border-radius: 0.5rem;
        padding: 0.375rem;

        --c-focus: var(--c-vif);
        --p-fonte: var(--p-champ);
    }

    .champ-texte > input[readonly] {
        cursor: not-allowed;

        --c-flux: var(--c-vif);
    }

    .champ-texte > input::placeholder {
        color: var(--c-plus);
        font-style: italic;
    }
]

@args [$etiquette, $nom, $requis : :), $valeur : '', $modifiable : :)]

<label
    .champ-texte
    for="champ-$nom"
>
    "$etiquette"
    <input
        #champ-$nom
        name="$nom"
        type="text"
        value="$valeur"
        [$requis] ? required
        [! $modifiable] ? readonly
    >

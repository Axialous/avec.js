@style [
    .section {
        flex: 0;
        scroll-margin-top: calc(var(--h-navigation) + 1rem);
    }

    .section.centree {
        flex-grow: 1;

        align-items: center;
        justify-content: center;
    }

    .section.centree > * {
        width: min(48rem, 100%);
    }
]

@args [$nom, $type : classique]

<section
    #$nom
    .section-$nom
    .section
    [$type = centree] ? .centree
>
    @stud
